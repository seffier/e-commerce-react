// MAIN

const MAIN_PATH = '/'

// AUTHENTICATION

const LOGIN_PATH = '/login'

export const ROUTES = {
    INDEX: { PATH: MAIN_PATH },
    AUTHENTICATION: {
        LOGIN: { PATH: LOGIN_PATH },
    }
}